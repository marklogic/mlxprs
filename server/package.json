{
  "name": "xquery-language-server",
  "description": "MarkLogic XQuery language server in node.",
  "version": "0.3.3",
  "license": "Apache-2.0",
  "engines": {
    "node": "*"
  },
  "dependencies": {
    "vscode-languageserver": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^6.0.73",
    "typescript": "^2.3.3"
  },
  "scripts": {
    "compile": "installServerIntoExtension ../ ./package.json ./tsconfig.json && tsc -p .",
    "watch": "installServerIntoExtension ../ ./package.json ./tsconfig.json && tsc --watch -p ."
  }
}
